diff --git a/node_modules/page-flip/src/Collection/PageCollection.ts b/node_modules/page-flip/src/Collection/PageCollection.ts
index 6ff9134..cf5d266 100644
--- a/node_modules/page-flip/src/Collection/PageCollection.ts
+++ b/node_modules/page-flip/src/Collection/PageCollection.ts
@@ -207,6 +207,7 @@ export abstract class PageCollection {
         if (this.currentSpreadIndex < this.getSpread().length) {
             this.currentSpreadIndex++;
             this.showSpread();
+            this.render.markStaticDirty();
         }
     }
 
@@ -217,6 +218,7 @@ export abstract class PageCollection {
         if (this.currentSpreadIndex > 0) {
             this.currentSpreadIndex--;
             this.showSpread();
+            this.render.markStaticDirty();
         }
     }
 
@@ -240,6 +242,7 @@ export abstract class PageCollection {
         if (spreadIndex !== null) {
             this.currentSpreadIndex = spreadIndex;
             this.showSpread();
+            this.render.markStaticDirty();
         }
     }
 
@@ -258,6 +261,7 @@ export abstract class PageCollection {
     public setCurrentSpreadIndex(newIndex: number): void {
         if (newIndex >= 0 && newIndex < this.getSpread().length) {
             this.currentSpreadIndex = newIndex;
+            this.render.markStaticDirty();
         } else {
             throw new Error('Invalid page');
         }
diff --git a/node_modules/page-flip/src/Render/Render.ts b/node_modules/page-flip/src/Render/Render.ts
index 927ee52..e084fb7 100644
--- a/node_modules/page-flip/src/Render/Render.ts
+++ b/node_modules/page-flip/src/Render/Render.ts
@@ -1,6 +1,6 @@
 import { PageFlip } from '../PageFlip';
 import { Point, PageRect, RectPoints } from '../BasicTypes';
-import { FlipDirection } from '../Flip/Flip';
+import { FlipDirection, FlippingState } from '../Flip/Flip';
 import { Page, PageOrientation } from '../Page/Page';
 import { FlipSetting, SizeType } from '../Settings';
 
@@ -83,6 +83,9 @@ export abstract class Render {
     /** Timer started from start of rendering */
     protected timer = 0;
 
+    /** Whether static pages need to be redrawn on the next frame */
+    protected needsStaticRedraw = true;
+
     /**
      * Safari browser definitions for resolving a bug with a css property clip-area
      *
@@ -109,6 +112,13 @@ export abstract class Render {
      */
     public abstract reload(): void;
 
+    /**
+     * Mark static pages as needing a redraw (e.g. after turnToPage).
+     */
+    public markStaticDirty(): void {
+        this.needsStaticRedraw = true;
+    }
+
     /**
      * Executed when requestAnimationFrame is called. Performs the current animation process and call drawFrame()
      *
@@ -130,7 +140,27 @@ export abstract class Render {
         }
 
         this.timer = timer;
-        this.drawFrame();
+
+        if (this.shouldRedraw()) {
+            this.drawFrame();
+
+            if (
+                this.animation === null &&
+                this.app.getState() === FlippingState.READ &&
+                this.flippingPage === null &&
+                this.shadow === null
+            ) {
+                this.needsStaticRedraw = false;
+            }
+        }
+    }
+
+    private shouldRedraw(): boolean {
+        if (this.animation !== null) return true;
+        if (this.needsStaticRedraw) return true;
+        if (this.flippingPage !== null || this.shadow !== null) return true;
+
+        return this.app.getState() !== FlippingState.READ;
     }
 
     /**
@@ -190,6 +220,7 @@ export abstract class Render {
      */
     public update(): void {
         this.boundsRect = null;
+        this.needsStaticRedraw = true;
         const orientation = this.calculateBoundsRect();
 
         if (this.orientation !== orientation) {
